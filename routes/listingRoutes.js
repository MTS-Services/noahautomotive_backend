const express = require("express");
const router = express.Router();

// TODO: Implement listing routes
// GET    /api/listings          - browse / filter listings (public)
// GET    /api/listings/:id      - single listing detail (public)
// POST   /api/listings          - vendor creates listing
// PUT    /api/listings/:id      - vendor edits own listing
// DELETE /api/listings/:id      - vendor deletes own listing

module.exports = router;
