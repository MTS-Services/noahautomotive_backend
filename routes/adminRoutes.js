const express = require("express");
const router = express.Router();

// TODO: Implement admin routes (role: ADMIN)
// GET    /api/admin/listings              - requested (pending) listings
// GET    /api/admin/listings/approved     - approved listings
// GET    /api/admin/listings/suspended    - suspended listings
// PUT    /api/admin/listings/:id/approve  - approve a listing
// PUT    /api/admin/listings/:id/suspend  - suspend a listing
// DELETE /api/admin/listings/:id          - delete a listing
// GET    /api/admin/vendors               - vendor list
// GET    /api/admin/users                 - all users

module.exports = router;
